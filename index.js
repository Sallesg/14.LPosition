function imgSlider(event) {
  document.querySelector('.html').src = event
}

function changeCircleColor(event) {
  const circle = document.querySelector('.circle')
  circle.style.background = event
}
